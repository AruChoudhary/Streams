//we will maintaing this history object as opposed to react router so that it will be easier to access it
// and change what page user is looking at


import { createBrowserHistory } from 'history'; 
export default createBrowserHistory();
